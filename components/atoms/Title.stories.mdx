import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks'
import Title from './Title'

<Meta title="Design System/Atoms/Title" component={Title} />

# Title

A very large title. Used for Archive and Post headings.

export const Template = (args) => {
  return (
    <div className="p-20 flex items-center justify-center">
      <Title {...args} />
    </div>
  )
}

<Canvas>
  <Story
    name="Title"
    args={{
      title: 'Eu strong lungo breve at mocha strong brewed.'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
