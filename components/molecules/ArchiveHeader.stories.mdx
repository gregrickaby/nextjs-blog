import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks'
import ArchiveHeader from './ArchiveHeader'

<Meta
  title="Design System/Molecules/Archive Header"
  component={ArchiveHeader}
/>

# Archive Header

Use this component to display the Archive Header.

export const Template = (args) => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <ArchiveHeader {...args} />
    </div>
  )
}

<Canvas>
  <Story
    name="Archive Header"
    args={{
      title: 'Archive Title',
      description: 'The archive header description should be a short sentance.'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
