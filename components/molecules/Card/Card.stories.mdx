import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks'
import Card from './Card'

<Meta title="Design System/Molecules/Card" component={Card} />

# Card

Use this component to display the Card.

export const Template = (args) => {
  return (
    <div className="p-20 flex items-center justify-center">
      <Card {...args} />
    </div>
  )
}

<Canvas>
  <Story
    name="Card"
    args={{
      data: {
        coverImage: '/blog/images/20191208-20191208-P1130781.jpg',
        category: 'Code',
        excerpt:
          "Learn more about the filter to modify a Custom Post Type after it's been registered.",
        title: "Modify a Custom Post Type after it's been registered"
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
