import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks'
import PageHeader from './PageHeader'

<Meta title="Design System/Molecules/Page Header" component={PageHeader} />

# Page Header

Use this component to display the Page Header.

export const Template = (args) => {
  return (
    <div className="p-20 flex items-center justify-center">
      <PageHeader {...args} />
    </div>
  )
}

<Canvas>
  <Story
    name="Page Header"
    args={{
      date: '2020-01-01',
      excerpt:
        "There's a filter which allows you to modify a Custom Post Type after it's been registered.",
      title: "Modify a Custom Post Type after it's been registered"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
