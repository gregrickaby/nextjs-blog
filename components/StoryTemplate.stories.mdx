import {Meta, Story, Canvas} from '@storybook/addon-docs/blocks'
import ArchiveHeader from '@/components/molecules/ArchiveHeader'

<Meta title="Template" component={ArchiveHeader} />

# Story Template

Duplicate this file and change the params to get started creating a new story.

export const Template = (args) => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <ArchiveHeader {...args} />
    </div>
  )
}

<Canvas>
  <Story
    name="Story Template"
    args={{
      title: 'Story Template',
      description:
        'Duplicate this file and change the params to get started creating a new story.'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
